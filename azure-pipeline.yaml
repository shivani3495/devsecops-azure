trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    mvn verify package sonar:sonar -Dsonar.host.url=https://sonarcloud.io/ -Dsonar.organization=azureorganization -Dsonar.projectKey=azureorganization-projectkey -Dsonar.login=0f8a8479dcabac66e2c1af87ce9bfd98de660b24
  displayName: "Integrate SAST using SonarCloud to populate code coverage in Azure DevOps DevSecOps Pipeline"
